# Screenshots

Drop these in here after running the pipeline against a real AWS account, then reference
them from the root `README.md`:

- `screenshot-pr-plan.png` — a PR showing the `terraform-plan` bot comment with plan output.
- `screenshot-prod-approval.png` — the `prod` GitHub Environment approval screen, and the
  resulting successful `terraform-apply` run.
